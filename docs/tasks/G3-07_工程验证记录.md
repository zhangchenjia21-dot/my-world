# G3-07 工程验证记录

Status: current
Scope: G3-07 Persistence Reality Test 的全部工程证据。最高状态 READY FOR INDEPENDENT REVIEW；不宣称 G3-GATE / Product PASS。

## 基线

- Start HEAD / origin/main：`cdd8511f7a097e54b1b38fbff9c14d08d1f29bdc`（worktree clean）
- Formal code base：`7e2e622f03782a1d66f5f8837d739f900615b775`（G3-06），当前 main 已含 G3-07 AGENTS.md 与 Task Packet
- Godot 4.7.2 console：`D:\AI\Engine\Godot_v4.7.2-stable_win64_console.exe`
- 测试根：`build/g3_07_reality`（隔离路径，runner 带安全校验）

## 实现前现实验证矩阵

- `docs/tasks/G3-07_实现前现实验证矩阵.md`（先于代码修改完成）

## DEC-05 UI 位置修改

- `src/main.tscn`：从 BottomBar 删除 DatabaseRecoveryButton；新增 `StartupFailureOverlay`（CenterContainer，Main 子节点，full-rect + offset_top=72 让出 TopBar/ExitButton，默认 hidden）→ PanelContainer → Margin → VBox → `StartupFailureTitle`（"无法继续当前游戏"）+ `%StartupFailureLabel` + `%DatabaseRecoveryButton`（unique name 不变，未复制第二个按钮）。
- `src/应用壳.gd`：physical_corruption / interrupted_recovery 分支显示 overlay、完整文案、按 `recovery_available` 控制按钮可见；healthy 分支隐藏；确认对话文案未改。
- `tests/g3_06/灾难恢复界面测试.gd`：断言适配到 overlay（原 status_label 断言更新）。

### 布局证据（`tests/g3_07/灾难恢复界面布局测试.gd`，真实窗口，failures=0）

- 1280×720 / Maximized(2560) / 960×540 三档：overlay 可见、中央说明可见、恢复按钮可见、按钮紧邻说明正下方（dy=12）、按钮水平居中（center == window_center）、BottomBar 无第二个恢复按钮、overlay 不盖 TopBar。
- 无 backup 分支：overlay 显示、按钮隐藏（不提供可点击恢复动作）。
- healthy 分支：overlay 与按钮均隐藏。
- 截图目检：`user://g3_07_shots/recovery_1280.png`、`recovery_maximized.png`、`recovery_960.png`。
- 确认文案保留"进度可能丢失窗口 / 损坏原件保留 / 区别于 Save-Load"。

## 确定性集成现实路径（`tests/g3_07/现实路径持久化测试.gd`，headless，failures=0）

- fresh Game → 14 seed Turn durable；recent-12：system + 12 对 == 25 条，最老保留 T03，T01 裁出；active 时 26 条，current user 恰好一次且在末尾。
- graceful close → reopen == resumed，同一 game_id，14 Turns 完整。
- named Save durable + backup refresh；Save id 可用。
- Future A（G307_FUTURE_A_ONLY ×2 Turns）→ Load S：durable 回 14 Turns，request 排除 A marker。
- Future B（G307_FUTURE_B_ONLY）→ Recover Previous Progress：精确恢复 A（16 Turns）、排除 B、request 含 A 不含 B。
- reciprocal Recover 回 B → 再 Recover 回 A，无 history 混线；最终 == Future A。
- Recover 后 reopen 同一 Game，truth == post-Recover A。
- reopen 后 Regenerate 抽验：request 以 user 结束、旧 assistant 排除、player 恰好一次、cancel 回滚不动 truth；Correction：corrected text 恰好一次在末尾、cancel 回滚未部分接受。
- backup 仍 discoverable，latest 文件存在。

### 实测耗时/体积

```json
{"db_size_bytes":81920,"graceful_close_ms":23,"reopen_after_recover_ms":6,"reopen_ms":5,"save_with_backup_refresh_ms":28}
```

无秒级卡顿；backup/reopen 均为毫秒级。

## 真实 DeepSeek 续玩（`tests/g3_07/真实续玩现实测试.gd`，blocking gate，failures=0，real_attempts=3）

- 12 个 deterministic seed durable。
- R1（pre-Save 真实 Turn）：attempt=1 accepted；request == system + 12 对 + current（跨 recent-12 临界，26 条）；current user 恰好一次且在末尾；persist-before-accept durable（13 Turns）；reopen 后同一 Game 且 R1 Turn 存在。
- named Save → Future A marker → Load S（回 13 Turns）。
- R2（post-Restore 真实 Turn）：attempt=1 accepted；request 排除 Future A marker、保留 restored accepted history；durable（14 Turns）。
- IR-01 修复后：R2 player input 显式包含 unique B-only marker `G307_FUTURE_B_REAL_ONLY`，且已断言该 marker 真实存在于 R2 accepted player history（排除类证据非空）。
- Recover Previous Progress：精确恢复 A、置换 R2 future；recovered accepted Conversation 含 A truth、排除 B-only marker。
- R3（post-Recover 真实 Turn）：attempt=1 accepted；request 含 A truth、排除 displaced B-only marker；durable（15 Turns）。
- 组合路径后 reopen 同一 Game，truth == post-Recover A 线（final entries 含 A、排除 B-only marker）；reciprocal Recovery 仍可用。

## 回归链（全部 failures=0）

- `tests/g3_06/运行真实验证.ps1 -SkipExport`：SQLite 在线备份契约 spike、数据库安全流程 focused、灾难恢复界面（适配后）、单写者验证、安全流程崩溃验证；自动链 g3_05 -SkipExport（含真实 Provider post-Recover 续玩）→ g3_04（Save/Load/Restore + 崩溃恢复）→ g3_03（迁移/生命周期/跨进程/UI-Context）→ g3_02（持久化流程/查询失败传播/post-COMMIT lost-ACK）→ g3_01（离线持久化/pre-COMMIT 崩溃）。
- G2 离线：g2_05 上下文组装、g2_04 会话域、g2_03 会话视图离线 —— 全部 PASS。
- G2-02 适配器冒烟（真实 key 注入进程 env，finally 恢复）：missing-key / deterministic transport failure / 真实 stream / 并发拒绝 / cancel / cancel 后新请求 —— 全 PASS，failures=0。
- 注：g3_04..g3_06 的 ps1 为无 BOM UTF-8，须用 `pwsh`（PS7）运行；PS 5.1 会按 ANSI 误读中文 `res://` 路径。未修改这些脚本。

## Windows export / 玩家启动

- `build/windows/my-world.exe` 经 `--export-debug "Windows Desktop"` 重建成功（export log 无 error/fail）。
- `run-game.ps1` 玩家路径：精确 exe 路径 + PID 匹配到主窗口（"my world (DEBUG)"），`IsZoomed=True`（6 次轮询一致，rect=1721×1033，默认 Maximized Window 非 Exclusive Fullscreen）；`CloseMainWindow` 优雅退出 HasExited=True。

## 安全 / scope check

- secret 扫描：无 `sk-` 明文、无真实 Authorization 值入库；唯一命中为既有的 `"Authorization: Bearer %s" % _api_key` 构造（未改）。
- `.env.local` 仅由 runner 读入进程 env 并在 finally 恢复；不打印、不提交；未用 setx。
- 未新增依赖；未新增 max_tokens / 输出长度上限 / 最小字数。
- 未实现 G4、schema v5、Timeline/backup browser、通用 framework。

## Bounded repair 说明

- 本次唯一产品代码修改为 DEC-05 恢复按钮位置（Owner 已确认）：main.tscn 结构调整 + 应用壳分支接线 + g3_06 测试断言适配。Reality Test 未暴露需要 schema/ownership 大改的 persistence defect。

## G3-GATE 前剩余风险

- Owner UAT（组合体验 + 新中央恢复按钮可发现性）尚未进行，不得由 Agent 代替。
- 真实 Provider 仅验证了深 history + Restore/Recover 后的 continuation；极端长 Session（数十 Turn 以上）未实测。
- recovery 按钮文案/视觉为最小可用，非最终美术。
